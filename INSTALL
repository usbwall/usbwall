# INSTALL

## Dependencies using distribution packages

Packages names may differ from those given behind depending on the version of your distribution.

### Linux/Debian

#### Compilation dependencies

sudo apt install cmake libusb-1.0-0-dev

#### Runtime dependencies

sudo apt install libusb-1.0-0

### FreeBSD and DragonFly BSD

#### Compilation dependencies

sudo pkg install cmake openldap-client

#### Runtime dependencies

sudo pkg install openldap-client

### NetBSD

#### Compilation dependencies

sudo pkgin install cmake libusb1

#### Runtime dependencies

### OpenBSD

usbwall is not yet available on OpenBSD because it does not make use of PAM and usbwall cannot work without PAM.
